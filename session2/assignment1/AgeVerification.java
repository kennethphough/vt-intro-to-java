package session2.assignment1;

import java.util.Scanner;

public class AgeVerification {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        // Prompt the user to enter their age
        System.out.print("Enter your age: ");
        // TODO: Declare and initialize a variable to store the user's age
        // int age = scanner.nextInt();

        // TODO: Use an if-else statement to check if the user is eligible to vote

        // If eligible, print "You are eligible to vote."
        // If not eligible, print "You are not eligible to vote."

        // Close the scanner
        scanner.close();
    }
}
