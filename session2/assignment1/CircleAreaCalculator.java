package session2.assignment1;

import java.util.Scanner;

public class CircleAreaCalculator {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);

        // Prompt the user to enter the radius of the circle
        System.out.print("Enter the radius of the circle: ");
        // TODO: Declare and initialize a variable to store the radius
        // double radius = scanner.nextDouble();

        // TODO: Calculate the area of the circle (area = π * radius * radius)
        // double area = Math.PI * radius * radius;

        // Display the result
        System.out.println(/* TODO: Add a message that displays the area of the circle */);

        // Close the scanner
        scanner.close();
    }
}
